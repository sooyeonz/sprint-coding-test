## [`문제번호`: `문제이름`] <- 지워서 사용

### 사용했던 방식 및 알고리즘
- ex) `List`를 사용해서 `for-each문`을 사용했다.

### 해결이 안 됐던 점
- ex) `for-each`문법이 헷갈렸다.

### AI 및 구글링에서 알려준 방식 및 알고리즘
```
List<Integer> numbers = new ArrayList<>();

for (int num : numbers) {
  if (num == 1) return num;
}
```
**알고리즘 설명**
